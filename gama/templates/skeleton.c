#include <gama.h>

// Load files like images and fonts here while loading screen shows
int load() { return 0; }

// Create shapes, images
int init() { return 0; }

// Update shapes, objects,
// theta is the time difference since last update
int update(float theta) { return 0; }

// Draw all the updated shapes manually
int render() { return 0; }

// When the user tries to exit the app
int shutdown() { return 0; }
