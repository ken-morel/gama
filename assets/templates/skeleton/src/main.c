#include "../gama/gama.h"

// Initialize the applicaiton here
void init(App *app) {}

// Create scenes, shapes, and bind them to the app
void create(App *app) {}

// When app closes
void shutdown(App *app) {}
