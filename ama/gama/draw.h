#pragma once

#include "color.h"
// #include "gapi.h"

int draw_rectantle(double x, double y, double w, double h, Color c) {
  // return gapi_draw_rect(x, y, w, h, gama_color_red(c), gama_color_green(c),
  //                       gama_color_blue(c), gama_color_alpha(c));
  return 0;
}
